<h1>PRODUCTS</h1>
<ul>
  <% @products.each do |product| %>
    <li>
      <%= product.name %><br>
      <%= product.price %><br>
      <%= product.description %><br>
      <button>Add to order</button>
    </li>
  <% end %>
</ul>
<%= link_to "Home Page", root_path%><br/>
<%= link_to "Orders Page", orders_path%>
